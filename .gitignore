# Node modules
node_modules/

# Local env files (keep .env.example in the repo; ignore secrets)
.env
.env.local
.env.*.local
.env.*.secret
.env.example

# Build / output folders
/dist/
/build/
/out/
/.serve/
/.vite/

# TypeScript build cache
*.tsbuildinfo

# Backend build output (if backend compiles to dist/build)
backend/dist/
backend/build/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Test coverage and test artifacts
coverage/
coverage/** 
.jest/
.nyc_output/

# Docker artifacts (local overrides, credentials)
docker-compose.override.yml
docker-compose.*.yml
.env.docker

# Editor / IDE
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.iml

# OS files
.DS_Store
Thumbs.db

# Caches
.cache/
.cache-loader/
.parcel-cache/

# Lockfiles: commit these for reproducible installs (do NOT ignore by default)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Misc
*.tgz
*.zip
*.tar
*.tmp
*.swp